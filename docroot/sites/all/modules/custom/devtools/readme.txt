Dev Tools use:
  
  Before taking advantage of any Dev Tools code you must load the appropriate
  component. Components are declared in devtools_components(), where the key
  of each array element is the name of the component.

  Components can be loaded by calling devtools_load($component);
  $component is optional and refers to the component name; if omitted, all
  components will be loaded.

